const { TestScheduler } = require('jest')
const getCSVReportFiles = require('../getCSVReportFiles.js')

describe('Obeter os relatórios de confirmados', () => {
    const defaultPath = ""
    const start = ""
    const countryGroup = ""
    const readFileCB = function(a, b, c, d) {
        
    }
    const writeFileCB = 

    test('obter todos os arquivos', done =>  {
        expect(1).toBe(3-2)
        done()
    })
})